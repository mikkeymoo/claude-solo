# claude-solo

Best-of Claude Code config — 7-stage pipeline, 28 smart agents, 25 hooks, 20 skills, and 9 starter rules in one install.

Combines the best patterns from the Claude Code community. No domain lock-in.

## Bootstrap a New Machine

Paste this into a fresh Claude Code session on any new machine. It detects Windows vs Linux/macOS and runs the right setup:

```
I need to set up my Claude Code environment from scratch using my claude-solo config repo.

Please do the following:

1. Check if git is available. If not, tell me to install it first.
2. Clone the repo:
   - Windows (PowerShell): `git clone https://github.com/mikkeymoo/claude-solo.git "$env:USERPROFILE\claude-solo"`
   - Linux/macOS/WSL: `git clone https://github.com/mikkeymoo/claude-solo.git ~/claude-solo`
3. Run the setup script:
   - Windows: `cd "$env:USERPROFILE\claude-solo"; .\setup.ps1`
   - Linux/macOS/WSL (Git Bash on Windows): `cd ~/claude-solo && bash setup.sh`
4. The setup will automatically install: uv (for Serena MCP), claude-code-cache-fix (prompt cache fix), the claude wrapper script, all hooks, agents, skills, rules, and the global MCP config (Serena + Playwright).
5. After setup completes, tell me what was installed and what (if anything) needs manual steps like API keys or tokens.

Detect my OS and run the correct commands. Ask me nothing — just do it.
```

---

## Install

**Windows (PowerShell 5+):**
```powershell
git clone https://github.com/mikkeymoo/claude-solo.git
cd claude-solo

.\setup.ps1              # Install globally (all projects)
.\setup.ps1 --project    # Install into current project only
.\setup.ps1 --both       # Install globally AND into current project
```

**Linux / WSL / macOS / Windows (Git Bash):**
```bash
git clone https://github.com/mikkeymoo/claude-solo.git
cd claude-solo

bash setup.sh             # Install globally (all projects)
bash setup.sh --project   # Install into current project only
bash setup.sh --both      # Install globally AND into current project
```

> **Requirements:** Python 3 on PATH. Node.js for hooks.

**Linux / WSL / macOS (Codex only):**
```bash
bash setup-codex.sh
bash setup-codex.sh --project
bash setup-codex.sh --both
```

**Linux / WSL / macOS (Claude + Codex together):**
```bash
bash setup-all.sh
bash setup-all.sh --project
bash setup-all.sh --both
```

**Windows (Codex only):**
```powershell
.\setup-codex.ps1
.\setup-codex.ps1 --project
.\setup-codex.ps1 --both
```

**Windows (Claude + Codex together):**
```powershell
.\setup-all.ps1
.\setup-all.ps1 --project
.\setup-all.ps1 --both
```

**Uninstall:**
```powershell
.\setup.ps1 --uninstall           # Remove from global
.\setup.ps1 --uninstall --project # Remove from project
```

Open Claude Code and you're done. Commands are prefixed `mm:` to avoid collisions with other skill sets.

---

## What Gets Installed

### Source-Agnostic Build
- Canonical content lives in `src/shared/commands/mm/*.md` and `src/shared/agents/*.md`
- Provider artifacts are generated by:
```bash
node scripts/render-providers.mjs
```
- Claude output: `src/commands/mm`, `src/agents`
- Codex output: `src/codex/skills`, `src/codex/agents`, `src/codex/AGENTS.md`

---

### Skills (Slash Commands) — 20 total

**The Sprint Pipeline (run in order):**
| Command | What it does | Time |
|---------|-------------|------|
| `/mm:brief` | Define scope + acceptance criteria | 15 min |
| `/mm:plan` | Atomic tasks, architecture, test matrix | 30 min |
| `/mm:build` | Implement in waves, atomic commits | 60-120 min |
| `/mm:review` | Staff-engineer review, auto-fixes critical issues | 30 min |
| `/mm:test` | Unit + integration + cross-platform tests | 30-45 min |
| `/mm:verify` | Hard pass/fail gate — lint, types, tests, secrets | 10 min |
| `/mm:ship` | Merge, verify deploy, monitor | 15-30 min |
| `/mm:retro` | What shipped, what to fix, next sprint | 15 min |

**Power Skills:**
| Command | What it does |
|---------|-------------|
| `/mm:troubleshoot` | Universal debug — build errors, test failures, CI, runtime, type errors, production incidents |
| `/mm:workflow` | Execution modes: `--auto` (hands-off), `--parallel` (waves), `--tdd` (red/green/refactor), `--quick` (rapid <2h) |
| `/mm:search` | Research + analysis: deep multi-source research, `--explain` code, `--estimate` effort |
| `/mm:doctor` | Project health: full check (default), `--map` codebase orientation, `--ready` pre-build gate |
| `/mm:session` | Session lifecycle: `save` (rich handoff), `save --lite` (quick pause), `restore`, `tokens` |
| `/mm:cleanup` | Code cleanup: regression-safe multi-pass (default) or `--audit` (find issues only, no changes) |

**Security:**
| Command | What it does |
|---------|-------------|
| `/mm:security` | Full security suite: OWASP audit + adversarial attacker review + enterprise compliance checklist |

**Quality & Audits:**
| Command | What it does |
|---------|-------------|
| `/mm:quality` | Quality suite: `--deps` CVEs/licenses, `--a11y` WCAG audit, `--migrate` DB migration, `--route` API testing |

**Release & Docs:**
| Command | What it does |
|---------|-------------|
| `/mm:release` | Full release: changelog from git, version bump, release notes, PR, tag, rollout checklist |
| `/mm:docs` | Docs suite: `sync` (default), `onboard`, `plan <feature>`, `update <feature>`, `distill [file]` |

**Scaffolding & Config:**
| Command | What it does |
|---------|-------------|
| `/mm:scaffold` | Project scaffold: `--python`, `--powershell`, `--sql` |
| `/mm:config` | Config suite: `--rules` path rules, `--schedule` cron tasks, `--github` GitHub App, `--ci` workflow, `--update`, `--help` |

---

### Agents — 28 total

**Core Specialists:**
| Agent | Role | Model |
|-------|------|-------|
| `senior-reviewer` | Specific, prioritized code reviews (🔴/🟡/🔵) | Opus |
| `security-auditor` | Enterprise security — OWASP, auth design, attack surface, SOC2 | Opus |
| `performance-optimizer` | Bottleneck diagnosis — profiles before optimizing, measures before/after | Opus |
| `database-architect` | Schema design, migrations, indexes, multi-tenant isolation | Opus |
| `system-architect` | Scalable architecture with focus on maintainability and long-term decisions | Opus |
| `root-cause-analyst` | Evidence-based root cause investigation — no guessing, read-only | Opus |
| `python-expert` | Production-ready, secure, high-performance Python (SOLID principles) | Opus |
| `release-manager` | Version bumps, changelogs, rollout checklists, rollback | Opus |
| `frontend-architect` | Accessible, performant UIs — React, component design, a11y | Opus |
| `sql-specialist` | SQL Server / T-SQL queries, schema design, index tuning | Opus |
| `migration-specialist` | Safe DB migrations — locking awareness, rollback plans, data integrity | Opus |
| `planner` | Turns vague requirements into executable atomic tasks | Sonnet |
| `debugger` | Systematic root-cause analysis, never guesses | Sonnet |
| `test-writer` | Tests that catch real bugs, not coverage theater | Sonnet |
| `git-workflow` | Branch strategy, atomic commits, conflict resolution, PRs | Sonnet |
| `ci-engineer` | GitHub Actions specialist — workflow design, caching, deploy gating | Sonnet |
| `api-designer` | REST API design — consistent shapes, correct status codes, versioning | Sonnet |
| `docs-librarian` | Documentation accuracy — drift detection, README updates, stale cleanup | Sonnet |
| `refactoring-expert` | Code quality improvement and technical debt reduction | Sonnet |
| `requirements-analyst` | Transform ambiguous ideas into concrete specs (read-only) | Sonnet |
| `technical-writer` | Clear, comprehensive docs tailored to specific audiences | Sonnet |
| `build-error-resolver` | Diagnose and fix TypeScript, bundler, module resolution errors | Sonnet |
| `circular-dependency-resolver` | Detect, analyze, and resolve circular imports | Sonnet |
| `type-error-analyzer` | Interpret, analyze, and resolve TypeScript type errors | Sonnet |
| `python-data` | Pandas/Polars, data pipelines, ETL, memory-efficient data work | Sonnet |
| `dependency-auditor` | CVE scanning, license audit, supply chain review (read-only) | Sonnet |
| `accessibility-auditor` | WCAG 2.1 AA audit with auto-fix for low-risk issues | Sonnet |
| `devops-engineer` | CI/CD, Docker, Kubernetes, GitHub Actions, environment config | Sonnet |

**Swarm Agents (for agent teams):**
| Agent | Role | Model | Key Feature |
|-------|------|-------|-------------|
| `swarm-lead` | Coordinator — decomposes tasks, spawns teammates, synthesizes results | Opus | Project memory, auto delegation |
| `swarm-implementer` | Code writer — focused implementation with atomic commits | Sonnet | Isolated git worktree |
| `swarm-researcher` | Read-only investigator — codebase analysis, API research, architecture review | Sonnet | Cannot modify files |
| `swarm-reviewer` | Senior reviewer — three-pass review (defects, edge cases, acceptance) | Opus | Auto-fixes critical issues |
| `swarm-tester` | Test specialist — runs suites, writes tests, reports regressions | Sonnet | Baseline-first testing |

---

### Hooks — 25 hook events (all automatic)

**Core Hooks:**
| Hook file | Event | What it does |
|-----------|-------|-------------|
| `session-start.js` | SessionStart | Injects git branch, sprint state, pending verification on session start |
| `session-end.js` | SessionEnd, Stop | Writes session summary to `.planning/SESSION-END.md` |
| `permission-request.js` | PermissionRequest | Conservative mode: auto-approves read-only ops, asks for the rest. Opt-in allow-all: `CLAUDE_SOLO_ALLOW_ALL=1` |
| `permission-denied.js` | PermissionDenied | Logs every denied operation to stderr so Claude knows what was blocked |
| `pre-tool-use.js` | PreToolUse | Warns about dangerous commands via stderr (advisory, never blocks); auto-wraps commands with `rtk` |
| `post-tool-use.js` | PostToolUse (Bash) | Logs commands, nudges RTK usage for token savings |
| `post-tool-use-failure.js` | PostToolUseFailure | Structured triage when a tool call fails — diagnose instead of looping |
| `build-checker.js` | PostToolUse (Edit/Write) | Hints to run `tsc --noEmit` or lint after editing TS/JS files. Opt-in: `CLAUDE_SOLO_BUILD_CHECK=1` |
| `error-handling-reminder.js` | PostToolUse (Edit/Write) | Flags empty catch blocks and swallowed exceptions in just-written code. Opt-in: `CLAUDE_SOLO_ERROR_REMINDERS=1` |
| `prompt-submit.js` | UserPromptSubmit | Auto-injects sprint context (BRIEF.md, PLAN.md) into every prompt |
| `pre-compact.js` | PreCompact | Saves checkpoint to `.planning/CHECKPOINT.md` before context compression |
| `post-compact.js` | PostCompact | Re-injects the checkpoint after compression — auto-resumes sprint state |
| `subagent-stop.js` | SubagentStop | Captures agent outputs as durable artifacts in `.planning/agent-outputs/` |
| `worktree-create.js` | WorktreeCreate | Copies `.env` and other gitignored files into new worktrees automatically |
| `file-changed.js` | FileChanged | Detects dep file changes mid-session (`package.json`, `pyproject.toml`, etc.) and warns |
| `config-change.js` | ConfigChange | Warns if claude-solo hooks are being removed from `settings.json` |
| `instructions-loaded.js` | InstructionsLoaded | Logs which `CLAUDE.md` and `.claude/rules/*.md` files are active |
| `cwd-changed.js` | CwdChanged | Logs directory changes to stderr so Claude knows the active project switched |
| `notification-idle.js` | Notification | Optional desktop/terminal notification when Claude finishes. Opt-in: `CLAUDE_SOLO_NOTIFY=bell\|os` |

**LSP-First Hooks (require cclsp MCP — enforce symbol navigation over Grep/Read):**
| Hook file | Event | What it does |
|-----------|-------|-------------|
| `lsp-first-guard.js` | PreToolUse (Grep) | Blocks Grep on camelCase/PascalCase/dotted code symbols — redirects to `find_references` or `find_workspace_symbols` |
| `bash-grep-block.js` | PreToolUse (Bash) | Blocks shell `grep`/`rg`/`ag`/`ack` on code symbols — allows `git grep`, non-code paths, non-code file types |
| `lsp-first-read-guard.js` | PreToolUse (Read) | 5-gate progressive system: requires LSP warmup first, 2 free reads, then blocks until LSP nav calls are made |
| `lsp-pre-delegation.js` | PreToolUse (Agent) | Blocks implementation agents launched without `## LSP CONTEXT` in prompt (subagents have no MCP access) |
| `lsp-usage-tracker.js` | PostToolUse (mcp__cclsp__*) | Tracks LSP tool usage per-project in `~/.claude/state/` — unlocks read gates after warmup + nav calls |

**Swarm Hooks (for agent teams):**
| Hook | Event | What it does |
|------|-------|-------------|
| `swarm/subagent-start` | SubagentStart | Injects git state, sprint context, and role-specific coordination rules into every agent |
| `swarm/teammate-idle` | TeammateIdle | Quality gate — blocks idle until work is committed/documented |
| `swarm/task-created` | TaskCreated | Blocks vague or oversized tasks, enforces atomic decomposition |
| `swarm/task-completed` | TaskCompleted | Verifies evidence of completion (git changes, test files, docs) |
| `swarm/stop-gate` | Stop | Prevents lead from stopping while teammates are active (opt-in via `--gate`) |

---

### Starter Rules — 9 rule files

Installed to `~/.claude/rules/` (never overwrites your existing rules):

| File | Covers |
|------|--------|
| `migrations.md` | Never modify applied migrations, rollback required, locking safety |
| `tests.md` | Test behavior not implementation, one assertion per test, no mocks of internal code |
| `ci-workflows.md` | Pin action SHAs, least-privilege permissions, cache dependencies, no secrets in logs |
| `env-files.md` | Never commit `.env`, validate at startup, document in `.env.example` |
| `api-routes.md` | Validate input at boundary, consistent error shapes, pagination, rate limiting |
| `security-sensitive.md` | No DIY crypto, constant-time comparison, parameterized queries, security headers |
| `config-files.md` | No `strict: false`, no blanket eslint-disable, pin tool versions |
| `documentation.md` | Update docs in same commit, document the why not the what, no stale TODOs |
| `lsp-first.md` | When cclsp MCP connected, use LSP tools over Grep for all symbol navigation |

Rules auto-apply when you edit files matching their path patterns via `/mm:rules` or Claude Code's native rules system.

---

### Hook Examples

**PostCompact auto-recovery** — when Claude compresses its context mid-session, the checkpoint is re-injected automatically:
```
[Context compression happened]
→ pre-compact.js saves .planning/CHECKPOINT.md
→ post-compact.js re-injects it: "Restoring sprint context: PLAN.md task 3/7, branch feat/auth..."
```

**WorktreeCreate auto-copy** — your `.env` is already in the worktree when a subagent starts:
```json
// .claude/worktree-copy-list  (one path per line)
.env
.env.local
secrets/service-account.json
```

**build-checker in action** — after editing TypeScript (requires `CLAUDE_SOLO_BUILD_CHECK=1`):
```
PostToolUse fires on file edit
→ build-checker.js detects tsconfig.json present
→ hints: "run: rtk tsc --noEmit to check for errors"
```

**notification-idle** — get a bell or OS notification when Claude finishes a long task:
```bash
CLAUDE_SOLO_NOTIFY=bell claude   # terminal bell
CLAUDE_SOLO_NOTIFY=os claude     # OS toast notification
```

**mm:rules — path-specific behavior:**
```bash
/mm:rules
# Claude creates .claude/rules/ and prompts:
# "Which path pattern? (e.g. src/migrations/**, tests/**, *.sql)"
# → you: "src/migrations/**"
# → Claude writes .claude/rules/migrations.md with your constraints
# Now whenever you edit src/migrations/*, those rules auto-apply.
```

---

### CLAUDE.md (appended, never overwrites)
- 7-stage sprint rules and order
- RTK token optimization command examples
- Cross-platform coding rules (Windows + Linux)
- Atomic git commit workflow
- Code quality principles (no premature abstraction, etc.)

---

## The 7-Stage Sprint

Every feature, every fix — follow the pipeline in order:

```
/mm:brief    →  scope it          (15 min)
/mm:plan     →  plan it           (30 min)
/mm:build    →  build it          (60-120 min)
/mm:review   →  review it         (30 min)
/mm:test     →  test it           (30-45 min)
/mm:verify   →  gate it           (10 min)
/mm:ship     →  ship it           (15-30 min)
/mm:retro    →  learn from it     (15 min)
```

**Total: 4-6 hours for a well-scoped feature.**

---

## Auto-Mode (Ralph Pattern)

Run Claude fully autonomously — no confirmation prompts, loops until done.

```bash
# Linux/WSL
bash run-auto.sh
bash run-auto.sh "implement the user auth endpoints from PLAN.md"
bash run-auto.sh --max 5

# Windows
.\run-auto.ps1
.\run-auto.ps1 "fix the failing tests"
```

**Requires**: Claude Code CLI installed + authenticated. Uses `--dangerouslySkipPermissions`.
**Stops when**: Claude outputs `TASK_COMPLETE` or max iterations reached.
**Use for**: well-scoped tasks with a clear PLAN.md.

---

## Swarm Mode (Agent Teams)

Run multiple Claude Code agents in parallel — coordinated through shared task lists, inter-agent messaging, and quality gate hooks.

### Quick Setup

```json
// Add to ~/.claude/settings.json
{
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  }
}
```

Or per-session:
```bash
CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 claude
```

### Start a Swarm

Just describe what you want in the Claude Code prompt — Claude picks agent types and team size:

```
Refactor the auth module and add a new settings page with password change and 2FA setup
```

```
Start an agent swarm to review this codebase for security issues and performance bottlenecks
```

```
Investigate why the app is slow after the last deploy. Have the team look at it from multiple angles.
```

### How It Works

```
You (user)
 └─> Swarm Lead (coordinator — decomposes work, tracks progress)
      ├─> Researcher (explores codebase, gathers context — read-only)
      ├─> Implementer A (writes code in isolated worktree A)
      ├─> Implementer B (writes code in isolated worktree B)
      ├─> Tester (runs tests, writes new test cases)
      └─> Reviewer (reviews all changes last — auto-fixes critical issues)
```

### Navigating Teammates

- **Shift+Down** — cycle through active teammates
- **Enter** — view a teammate's full session
- **Escape** — interrupt a teammate's current turn
- **Ctrl+T** — toggle the shared task list

### Launcher Script (Alternative)

```bash
bash run-swarm.sh "Implement the notification system"
bash run-swarm.sh --teammates 5 "Refactor the auth module"
bash run-swarm.sh --split "Review PR #42 from 3 angles"    # tmux panes
bash run-swarm.sh --gate "Build the payment flow"           # block premature stop

.\run-swarm.ps1 "Implement the notification system"
```

### Swarm vs Subagents vs Auto-Mode

| | Auto-Mode | Subagents | Swarm |
|---|---|---|---|
| **Agents** | 1 | 1 + helpers | 3-6 parallel |
| **Best for** | Sequential execution | Quick focused tasks | Complex parallel work |
| **Token cost** | Lowest | Low | Higher |
| **File conflicts** | N/A | N/A | Prevented via worktree isolation |

For full details: [docs/swarm-guide.md](docs/swarm-guide.md)

---

## Global vs Project Install

| Scope | Where | When to use |
|-------|-------|-------------|
| `global` (default) | `~/.claude/` | Your config for all projects |
| `--project` | `./.claude/` | Project-specific overrides |
| `--both` | Both | Override global with project-specific customization |

**Tip**: Install globally first, then use `--project` for per-project customization.

---

## Safe to Install (Brownfield-Friendly)

claude-solo is designed to install alongside your existing Claude Code config without breaking anything.

| File | Behavior | Your data safe? |
|------|----------|----------------|
| **CLAUDE.md** | Appends inside `<!-- claude-solo:start/end -->` markers. Your content is untouched. | ✅ Yes |
| **settings.json** | Merges missing keys only — adds hook events that don't exist yet. Never overwrites. | ✅ Yes |
| **mcp.json** | Copies template only if file doesn't exist. Won't overwrite. | ✅ Yes |
| **rules/*.md** | Copies only if the file doesn't exist. Never overwrites your rules. | ✅ Yes |
| **Agents** (`.md`) | Overwrites files with matching names. Your custom agents with different names are untouched. | ⚠️ Name collisions |
| **Hooks** (`.js`) | Overwrites files with matching names. | ⚠️ Name collisions |

### Automatic backups

Before overwriting anything, the installer backs up all existing files that would be replaced:

```
~/.claude/.claude-solo-backup/20260406-143022/
├── hooks/
│   └── pre-tool-use.js      (your original)
├── agents/
│   └── debugger.md           (your original)
├── settings.json
└── CLAUDE.md
```

To skip backups: `bash setup.sh --no-backup` or `.\setup.ps1 -nobackup`

### Permission hook behavior

By default, `permission-request.js` runs in **conservative mode** — auto-approves read-only operations (Read, Glob, Grep, git status/log/diff, etc.) and asks for everything else.

To switch to allow-all mode (auto-approves everything except catastrophic commands):
```bash
CLAUDE_SOLO_ALLOW_ALL=1 claude
```
Or add to your shell profile / `settings.json` env block.

---

## Optional: MCP Servers

claude-solo works without any MCPs. A template (`mcp.json`) is installed with 8 pre-configured servers — all disabled by default:

- **cclsp** — LSP navigation for code symbols (`find_definition`, `find_references`, `get_diagnostics`). Enables the LSP-first hooks. Requires `npm install -g cclsp` + language servers (`pip install python-lsp-server` for Python)
- **GitHub** — PRs, issues, Actions, code search
- **Playwright** — browser automation and E2E testing
- **PostgreSQL** — read-only database access
- **Sentry** — production error tracking
- **Brave Search** — web search (free tier: 2000/month)
- **Memory** — persistent knowledge graph
- **Filesystem** — file access with configurable allowed directories

Recommended: enable 2-3 max. Details: → [docs/recommended-mcps.md](docs/recommended-mcps.md)

---

## What This Doesn't Include

- No domain-specific tools
- No openclaw (ever)
- No premium tier, no telemetry, no accounts

---

MIT licensed.
